const { validateLogin, validateRegister } = require('./validateRegistration');
const validateExpense = require('./validateExpense');

module.exports = {
  validateLogin,
  validateRegister,
  validateExpense,
};
